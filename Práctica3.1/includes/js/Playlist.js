"use strict"

$(document).ready(function () {
    $(".etiqueta").on("click", function (event) {
        
        let texto = $(event.target).text();
       

    });

    $(".etiqueta").on("click", function (event) {
        
      let texto = $(event.target).text();
     

  });
});